<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helloworld"
    android:targetSandboxVersion="2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/AppTheme" >
        <activity android:name="com.example.helloworld.MainActivity" > <!-- #调用逻辑。这里的.MainActivity的.就是指包名==package，即第三行代码的属性值 -->
            <!-- 项目启动入口(打开后的第一个页面) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- #指定动作 -->

                <category android:name="android.intent.category.LAUNCHER" /> <!-- #指定分类 -->
            </intent-filter>
        </activity>
        <activity android:name="com.example.helloworld.RelativeLayout_1" />
        <activity android:name="com.example.helloworld.textViewActivity" />
        <activity android:name="com.example.helloworld.ButtonActivity" />
    </application>

</manifest>